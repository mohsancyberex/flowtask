name: flowtask
description:  task management app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.10 
  animate_do: ^3.1.2
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  intl: ^0.18.1
  confetti: ^0.7.0
  go_router: ^17.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true